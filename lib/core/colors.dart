import 'package:flutter/material.dart';

class AppColors {
  // Dark mode backgrounds
  static const background = Color(0xFF1C1422);
  static const card = Color(0xFF271D30);

  // Primary accent — soft rose-pink
  static const primary = Color(0xFFE8709A);

  // Emergency — vivid coral-red
  static const emergency = Color(0xFFE8475E);

  // Text
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFBFAAC8);

  // Accent palette for gradients & highlights
  static const lavender = Color(0xFFC59BF2);
  static const blush = Color(0xFFF7A7B8);
  static const peach = Color(0xFFFFB7A6);
  static const mintAccent = Color(0xFF7ED8C4);

  // Light-mode counterparts
  static const backgroundLight = Color(0xFFFFF0F4);
  static const cardLight = Color(0xFFFFF8FA);
}