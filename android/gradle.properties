org.gradle.jvmargs=-Xmx2048M -XX:+UseSerialGC -XX:ReservedCodeCacheSize=240m -XX:CICompilerCount=2
android.useAndroidX=true
android.enableJetifier=true
systemProp.org.gradle.internal.http.connectionTimeout=120000
systemProp.org.gradle.internal.http.socketTimeout=120000
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Disable parallel builds to prevent out-of-memory crashes on resource-constrained systems
org.gradle.parallel=false

# Kotlin compiler daemon memory configuration
kotlin.daemon.jvmargs=-Xmx1024M -XX:+UseSerialGC -XX:CICompilerCount=2
kotlin.compiler.execution.strategy=in-process

# Disable Gradle daemon to save memory
org.gradle.daemon=false

